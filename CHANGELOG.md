# 更新日志

## v2.1（20260822）

这一版把手动配置时攒下的经验搬进工具，核心是四件事，都对着 pi 0.84.2 的源码核对并实测过。

### 混合协议不再拆服务商

pi 取地址时模型级 baseUrl 优先于服务商级（provider-composer.js），所以一个服务商里完全可以混用两种协议：协议不同的模型自带 api 和 baseUrl 就行。以前工具会拆出 xxx-claude 这样的额外条目，现在默认不拆，pi 的 /model 列表干净很多。需要给两种协议配不同伪装头时，勾上“协议不同的模型拆成独立服务商”回到旧行为。

### 第三方中转兼容预设

pi-ai 对这两项用的是白名单判断：不在名单里的中转会被当成标准 OpenAI，发 developer 角色、发 max_completion_tokens，而多数中转两样都不认。现在高级选项里有一组预设，默认勾上关闭 developer 角色和改用 max_tokens，写在服务商级 compat。另外两项可选：容忍缺少 finish_reason（未公开字段，pi-ai 运行时确实读它）、Claude 格式关闭工具输入流式。导入已有服务商时预设会按原配置回填，不会偷偷改动人家手调过的 compat。

实测这一侧也补了个方向：以前只在中转拒绝 max_tokens 时才记一笔，现在实测认 max_tokens 就明确写进配置，否则 pi 按自己的默认发另一个字段仍然会挂。

### 实测节奏防反探测

有的中转 60 秒内测 5 个不同模型就封 IP。默认从并发 3 改成串行加 10 秒间隔，界面显示预计耗时；选并发大于 1 会先确认。识别到限流、批量探测、封禁类信号自动停下整轮并提示等待。

### 协议错配直接判出来

中转常见“标着支持 Claude 接口，实际返回 OpenAI 格式”，pi 会解析失败。以前工具只说一句“请求成功但没返回内容”，现在会明确判失败，说明真实格式，并给一键修复（改接口格式，同时自动改对地址）。

### 其他

1. 模型识别把 -cc 后缀纳入归一化，minimax-m3-cc 这类中转改名能精确匹配回官方型号。
2. 写入前多一道沙盒预演：把候选配置写到临时目录、临时改子进程家目录，让真正的 pi 读一遍。schema 校验只管 JSON 结构，pi 组装模型时缺 api、缺 baseUrl 会把该服务商整包丢掉，这一层只有真跑才看得见。
3. 新增 tools/tidy_models_json.py：整理现有 models.json，规范地址、按官方参数库校准数值、补价格、补兼容项、清理无效字段，可选删除或合并服务商，默认只演练。

## v2.0（20260822）

这一版的主题是**别把配置写坏**，其次是好用和可维护。

### 修掉的真 bug

1. **高级选项里的示例请求头被当真写进了配置**。文本框预填的 {"User-Agent": "..."} 本身是合法 JSON，而写入时无条件读这个框，于是每个用工具写入的服务商都被塞了一个假的 User-Agent。现在文本框默认留空，示例移到说明文字里，写入时还会过滤掉三个点这类没填完的占位值。
2. **空密钥会让整个 models.json 被 pi 拒绝**。pi 的 schema 要求 apiKey 至少 1 个字符，写空会导致整份配置作废、所有服务商一起失效。原来的提示只说“这个服务商将不可用”，实际后果严重得多。现在直接拦住，提示文案也改对了；生成配置时空密钥一律省略字段而不写空字符串。
3. **导入已配置服务商再写入会丢字段**。原来只读回 baseUrl、apiKey、api 和 models，重写时把 provider 级的 name、compat、modelOverrides、oauth、headers、authHeader 全丢了。现在这些字段原样保留，headers 和 authHeader 也会回填到界面上，预览页会写明保留了什么。
4. **表格“思考档位”列把“默认”显示成“不可用”**。官方参数库里 1004 个支持思考的模型有 604 个没写 thinkingLevelMap，这些行原来一律显示成七档全不可用，与实际写入结果和编辑对话框的显示互相矛盾。
5. **管理窗口遇到重复模型 ID 直接打不开**。行号用 hash 生成，同 ID 撞车后 Treeview 抛异常，异常发生在窗口初始化里。现在改用自增序号加映射表。
6. **后台线程里读 tkinter 变量**。识别线程里调用 var_api.get() 属于跨线程访问 Tcl，可能偶发卡死。现在值在主线程取好再传进线程。
7. **实测里 max_completion_tokens 的自动重试只覆盖第一项**。中转只认这个字段时，会出现“可用性通过、图片和上下文全失败”的误报。现在一轮测试里探测到一次就整轮记住。
8. 实测请求体里不再混入内部字段 _model_id（原来会连它一起发给服务商）。
9. 服务商标识的合法性校验改成 ASCII，与提示文案一致（原来的正则会放过中文）。

### 新增的安全网

1. **写入前自检**。按 pi 的 schema 检查密钥、input 取值、cost 四项价格、thinkingLevelMap、headers 类型等，发现致命问题直接阻止写入，不留下半成品。只拦本次改动引入的新问题，配置里原本就有的毛病只提醒，不影响删改。
2. **借 pi 自己的校验器复核**。本机装了 pi 和 Node 时，写盘前把候选配置交给 pi 的校验器验一遍，它说不合法就不写。
3. **写入后让 pi 实际读一遍**。执行 pi --list-models，把 pi 认到的模型列出来；万一 pi 报错，弹窗上有“还原到写入前”一键回滚。
4. **备份治理**。只保留最近 10 份备份，新增备份管理窗口，可以看清每份备份的时间、大小、包含哪些服务商，并选一份还原；还原前会先备份当前配置。
5. **密钥不再经命令行传递**。借 Node 发请求时改成用标准输入传参，避免同机其他进程从进程命令行里看到密钥；错误信息里的密钥也会被抹掉。
6. 主界面密钥框默认打码显示，可勾选临时显示。

### 好用度

1. 主表格加了搜索框、筛选（已识别、未识别、已配置、已勾选）和列排序，几百个模型也能快速找。全选和全不选只作用于当前筛选出来的行。
2. 管理窗口可以直接双击修改已有模型的参数，不用删掉重新加。
3. 实测支持并发（默认 3），图片、思考、上下文三项可以单独关闭。
4. 实测结论可以一键回写配置：关掉实测不支持的图片、补上实测支持的图片、写入 compat.maxTokensField、关掉被明确拒绝的思考。
5. 界面标题带版本号；状态栏写明参数库来自本机 pi 实时数据还是内置快照，以及对应的 pi-ai 版本。

### 工程化

1. 自检从 main.py 里的一串 assert 换成 tests 目录下的 156 个单元测试，覆盖匹配、生成、写入、备份、校验、拉取、实测和界面逻辑。测试一律强制用仓库里的快照，断言尽量从参数库现取，快照升级不会无故变红。python main.py --selftest 仍然可用，跑的就是这套测试。
2. 新增三个 GitHub Actions 工作流：推送跑测试；打 v 开头的 tag 自动打包 exe 并发 Release（打包后还会跑一次 exe 自检）；每周自动从 npm 更新官方参数库快照并提交。
3. 参数库路径探测从一条硬编码路径扩展为多条候选（含扁平安装、自定义 npm 前缀、pi 命令所在目录），并支持用环境变量 PIMCT_CATALOG_DIR 手动指定。
4. ui.py（1438 行）拆成 ui 包：common 公用常量、app 主窗口、dialogs 各类对话框、manage 管理窗口、results 实测结果窗口。
5. 删掉 fetcher 里内嵌的 Node 脚本副本，nodefetch.mjs 成为唯一来源，避免两份代码漂移。
6. matcher 的归一化索引改由 Catalog 缓存，不再每次匹配都重建。
