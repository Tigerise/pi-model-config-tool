# pi 模型配置工具使用说明

## 这个工具是干什么的

帮你把第三方中转服务商的模型配置进 pi。以前要手工填上下文长度、思考等级、多模态这些字段，填错一个模型就不好用；现在工具会自动去 pi 自带的官方参数库（1267 个型号）里查，把所有字段配好再写进配置文件。

写入前后都有防护：写盘前先按 pi 的规则自检一遍，本机装了 pi 还会调用 pi 自己的校验器复核，再在临时沙盒里让真正的 pi 试读一遍（完全不碰你真实的配置）；写完立刻用 pi 实际读一次，把 pi 认到的模型列出来给你看。这条防线很重要，因为 **pi 读配置是全有全无的：只要一处字段非法，整份 models.json 都会作废，连工具没碰过的服务商也一起消失**。

## 怎么打开

双击文件夹里的 **pi模型配置工具.exe** 就行，不需要安装任何东西。

第一次打开时 Windows 可能会提示“已保护你的电脑”，点“更多信息”，再点“仍要运行”。这是没有数字签名的自制小工具的正常提示。

## 操作步骤

**第一步**：填三样信息。服务商标识是给它起的名字（字母或数字，比如 mysvc、relay2）；接口地址和密钥用服务商给你的；接口格式不确定就选“自动检测”。密钥框默认打码显示，需要核对时勾一下右边的“显示”，录屏和截图时不用担心泄露。

密钥**不能留空**。pi 要求密钥至少一个字符，写成空的会让整份配置作废，所以工具会直接拦住不让写。

**第二步**：点“拉取模型列表”。工具会自动尝试三种格式，成功后地址栏会自动修正成规范写法，然后逐个识别每个模型对应的官方参数。

**第三步**：在表格里勾选要用的模型。每行能看到识别结果（含置信度）、上下文长度、最大输出、思考档位、能不能看图。**双击任何一行可以修改参数**，识别错了还能在里面搜索官方型号重新匹配。没被识别的行会标出来，需要手动补数值。

中转返回几百个模型时，用表格上方的**搜索框和筛选下拉**：搜索按模型 ID 或显示名过滤，筛选可以只看已识别、未识别、已配置或已勾选的行，点列标题还能按 ID、上下文长度、最大输出排序。注意“全选”和“全不选”只作用于当前显示出来的行，所以可以先搜 claude 再全选，只勾一类模型。

思考档位那一列写“默认”的意思是这一档不写进配置，交给 pi 按厂商惯例处理，**不等于不支持**。写“不可用”才是官方明确标了这一档用不了。

**关于接口格式**：有些中转站里大部分模型走 OpenAI 协议，但个别 Claude 模型只支持 Anthropic 协议。工具默认会自动处理：拉取列表后，识别到 Claude 系模型（含中转常用的 -cc 后缀）会自动标成 Claude 协议，模型 ID 后面会显示标记。写入时**不再拆成两个服务商**，而是让这些模型自带接口格式和地址，一个条目里混着用，pi 的 /model 列表更干净。地址规则工具替你算好：OpenAI 格式带 /v1，Claude 格式不带（软件自己拼接），Gemini 格式带 /v1beta。

只有一种情况需要拆开：两种协议要用不同的伪装请求头（请求头只能配在服务商级）。这时勾上主界面的“协议不同的模型拆成独立服务商”。

如果你的中转两种协议都支持、想全部跟随服务商格式，把主界面上的“Claude 系模型自动改走 Claude 协议”勾去掉即可；单个模型也可以双击后在“接口格式”里手动改。

**刷新已有服务商（补充新模型）**：主界面点“导入已配置…”，选一个服务商，工具会自动填好地址、密钥、自定义请求头和 authHeader 开关，把已配置的模型带进表格并勾选，然后自动拉取最新列表。新出现的模型以未勾选状态追加在表格里，勾上想要的写入即可；已配置的保持勾选就不会丢。取消勾选某个已配置模型再写入，等于把它从该服务商移除。这条路径上原有的 name、compat、modelOverrides 等工具不管理的字段会原样保留，预览页里也会明确写出来。

**合并与拆分规则**：写入时工具会自动判断归属。同名同密钥直接更新；名字没用过但地址、密钥、接口格式都与某现有条目一致就并入那个服务商，不会重复建；地址相同但密钥不同，确认后独立新建；同名不同密钥会自动改用新名字（加 -2 后缀）避免覆盖。每次写入的预览页顶部会列出变更摘要：将新增哪些、将移除哪些、自动保留了哪些。

**清空重置**：主界面的“清空重置”按钮一键清掉服务商标识、地址、密钥和整个表格，换一家重新来。

**第四步**：点“写入 pi 配置”。会先自检合法性，然后弹出预览让你确认，确认后自动备份原文件再写入。其他服务商不受影响。如果你的配置里原本就有 pi 不认的地方，工具不会因此拦着你删改，只会在预览里提醒一句。

写完会弹出一个复核窗口，里面是 pi 实际认到的模型清单。万一 pi 报错，窗口上有“还原到写入前”按钮，一键退回。

写完在 pi 里按 /model 就能看到新模型，不需要重启。

## 管理已有配置：改参数和删东西

主界面点“管理 / 删除已有配置”会打开管理窗口：

1. 左边列出配置文件里所有服务商，点某个服务商的名字，右边就会显示它下面的所有模型。窗口支持最大化、最小化和自由拖拽调整大小。
2. 点服务商那一行的行首方格，是把整个服务商标记为删除；在右边点模型的行首方格，是把这个模型从该服务商里删掉。可以跨多个服务商连续标记，标错再点一下就取消。
3. **双击右边的模型可以直接改它的参数**（上下文长度、最大输出、思考档位、图片、接口格式，也能重新搜官方型号覆盖），不用像以前那样删掉重新加一遍。改过的行会标蓝色并显示“已改”。
4. 所有标记和修改都只是暂存，底部会显示待应用汇总，点“应用更改”后会先弹出预览，确认后自动备份原文件、自检合法性再写入。
5. 如果配置文件在别处被改过（比如手动编辑过），点“重新加载”刷新显示。

## 怎么测试服务商和模型好不好用

在管理窗口里选中左边某个服务商，下方是实测控制条：

1. **测试本服务商全部模型**：逐个测试该服务商下的每个模型，结果实时显示在弹出的结果窗口里。
2. **测试选中行（可 Ctrl 多选）**：只测你点选的几行。
3. **停止测试**：随时中断。
4. **节奏**：默认串行、每个模型之间等 10 秒。有的中转会反探测，短时间内连测多个模型就封 IP，这个默认值就是为它准备的，界面会显示预计耗时。赶时间可以调并发，但选到 2 以上会先让你确认。
5. 一旦服务商返回限流、批量探测、封禁这类信号，工具会自动停下整轮测试并提示等一段时间再来，不会继续踩。
6. 图片识别、思考能力、上下文抽查三项都能单独勾掉，只想快速看看哪些模型还活着时全关最省 tokens。

默认勾选“上下文抽查”时，每个模型会额外发一段约 4000 tokens 的填充文本验证长文本能力，**这会多消耗一些 tokens**（按常见价格约几分钱以内）。

每个模型测四到五项：

1. **可用性**：发一条极短对话，看能否正常返回、延迟多少毫秒。
2. **协议匹配**：中转常有“标着支持 Claude 接口、实际返回 OpenAI 格式”的模型，pi 遇到会解析失败。工具会看返回体到底是哪种格式，对不上就直接判失败并给一键修复。
3. **图片识别**：发一张纯色小图问什么颜色。能答对说明图片链路通；纯文字模型拒图会标灰色跳过，属于正常。
4. **思考能力**：按模型配置发思考参数，看是否被接受、有没有返回思考内容。部分中转会把思考内容藏起来，这种情况会标黄色提醒。
5. **上下文抽查**：验证长文本输入是否畅通，并显示服务端实际计费的 tokens 数。**注意这是抽查，不代表标称上下文全长**，完整实测要发送几十万 tokens，费用上不现实。

每个模型最后有一行加底色的“结论”：失败表示基本不可用，建议删除；警告表示能用但有疑点（比如标称支持图片实测却拒绝），自己权衡；通过表示各项正常。

**把实测结论应用到配置**：结果窗口底部这个按钮会把测出来的事实变成待应用的修改，比如实测拒绝图片就取消图片勾选、实测支持图片就补上、最大输出字段按实测结果写进兼容项、思考参数被明确拒绝就关掉思考、协议错配就改接口格式并同步改对地址。点它只是暂存，回到管理窗口点“应用更改”才真正写盘。

测试时会自动识别模型级接口格式：如果某个模型单独指定了 Claude 或 Gemini 格式，测试会按它自己的协议和对应地址发请求，和 pi 实际行为一致。

## 备份和还原

每次写入前工具都会自动备份，文件在配置旁边，名字是 models.backup-日期时间.json（在 C:\Users\用户名\.pi\agent\ 下面）。**只保留最近 10 份**，不会无限堆积。

主界面和管理窗口都有“备份管理”按钮：能看到每份备份的时间、大小、包含哪些服务商，选一份点“还原选中备份”就覆盖回去。还原本身也会先把当前配置备份一次，所以还原也是可撤销的，不用担心点错。

## 几个重要说明

**中转兼容预设**在高级选项里，默认勾上两项：关闭 developer 角色、最大输出字段用 max_tokens。原因是 pi 会把不认识的中转当成标准 OpenAI 服务，发 developer 角色和 max_completion_tokens，而大多数中转两样都不认，配好了却用不了多半就是这个。另外三项可选：容忍缺少 finish_reason（聚合站常见）、Claude 格式关闭工具输入流式、允许无签名的思考块。导入已有服务商时这些勾选会按你原来的配置回填，不会擅自改动。

**pi 只认服务商级的请求头**，写在单个模型上的请求头会被 pi 忽略。密钥也不用写进请求头，勾“自动添加 Bearer 认证头”即可，免得同一把密钥在文件里存两份。

**自定义请求头默认是空的**。需要伪装客户端（比如模拟 Claude Code）时展开“高级选项”，按 JSON 格式填，例如 {"User-Agent": "claude-cli/2.1.0 (external, cli)", "x-app": "cli"}。留空就是不加任何自定义头。值写成三个点这类明显没填完的内容会被自动忽略。

**视频识别**：pi 的配置只认文字和图片两类输入，没有视频对应的字段，所以界面上只有“能识别图片”一个选项。

**拉取失败怎么办**：

1. 先确认密钥有没有效。有的服务商密钥过期或欠费时会报 401。
2. 有的服务商屏蔽了模型列表接口但对话正常，这时点“手动粘贴 ID”，从服务商后台复制模型名单过来，一样能自动识别参数。
3. 需要伪装请求头的中转，展开“高级选项”填 JSON 格式的请求头。

**关于本机的 Node.js**：工具拉列表时优先借用你电脑上的 Node 发请求，因为不少中转前面的防护墙专门拦截 Node 以外的客户端，而 pi 本身就是 Node 程序，这样兼容性最好。删掉 Node 后工具也能工作，只是个别难缠的中转会连不上。密钥是通过标准输入交给 Node 的，不会出现在进程命令行里。

**参数库来源**：装了 pi 就直接读 pi 安装目录里的实时参数库，界面底部状态栏会写明来源和版本；没装 pi 就用内置快照兜底。快照过期不影响装了 pi 的用户。

## 文件夹里都有什么

pi模型配置工具.exe 是主程序，dist 文件夹里是打包的原始输出。snapshot 里是离线官方参数库快照。py 文件和 ui 文件夹是源代码，tests 是自动化测试，tools 里是更新快照的脚本。venv 和 build 是打包用的临时环境，删了不影响 exe 使用。

## 想卸载的话

直接删除整个项目文件夹即可，系统里不会有任何残留。它只在运行时读取你的 pi 配置，不往注册表和系统目录写东西。注意删之前确认不再需要 models.backup 开头的备份文件（它们在 C:\Users\用户名\.pi\agent\ 下面，不在本文件夹）。
